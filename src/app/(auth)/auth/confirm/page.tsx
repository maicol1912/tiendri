import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Confirmar acceso | Tiendri",
  robots: { index: false, follow: false },
};

export default function AuthConfirmPage() {
  return <div>Auth Confirm Page</div>;
}
