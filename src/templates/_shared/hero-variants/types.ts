export interface HeroLayoutProps {
  subtitle: string;
  titleLight: string;
  titleBold: string;
  description: string;
  ctaText: string;
  image: string;
  bgColor: string;
  onCtaClick?: () => void;
}
