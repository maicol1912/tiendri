// TODO: Build CSS custom properties from store theme config

export function buildCssVars(
  _themeConfig: Record<string, string>
): Record<string, string> {
  // TODO: Transform theme config into CSS variable key-value pairs
  return {};
}
